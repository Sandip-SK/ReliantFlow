apiVersion: v1
kind: Service
metadata:
  name: reliantflow
  labels:
    app: reliantflow
spec:
  selector:
    app: reliantflow
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP